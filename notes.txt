sdkmanager --install emulator
export PATH="$PATH:/usr/local/android/emulator

sdkmanger --install "system-images;android-33;google_apis;x86_64"
# more with google_apis_playstore

avdmanager create avd -n test -k "system-images;android-33;google_apis;x86_64"

apt update && apt install libpulse0

# emulator -avd test -no-accel
